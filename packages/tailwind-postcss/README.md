# @tailwindcss/postcss shim

This workspace package provides a thin wrapper around the `tailwindcss`
PostCSS plugin so that workspaces that expect `@tailwindcss/postcss`
to be present can resolve it locally without relying on an external
package.
