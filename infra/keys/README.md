# Development Keys

This directory stores development-only RSA key pairs used for signing playback tokens. Replace with production-grade keys in secure storage before deployment.

- `dev.key` – private key (never check into production repo)
- `dev.pub` – public key distributed to playback services
