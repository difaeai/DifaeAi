{
  "name": "@difae/windows-bridge",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc --project tsconfig.json && pkg dist/index.js --targets node18-win-x64 --output dist/difae-bridge.exe"
  },
  "dependencies": {
    "node-windows": "^1.0.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1",
    "rimraf": "^5.0.5",
    "typescript": "^5.9.3"
  }
}
