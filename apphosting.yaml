# apphosting.yaml
runConfig:
  cpu: 1
  memoryMiB: 512
  maxInstances: 2

env:
  # Tell the buildpack to run npm/yarn inside the backend folder
  - variable: BP_NODE_PROJECT_PATH
    value: backend
    availability:
      - BUILD
