# Firebase App Hosting backend configuration

# Tell Firebase the real backend folder
workspace: backend

# Build steps executed inside the backend folder
build:
  - run: npm install
  - run: npm run build

# Start command
run: npm run start

# Resource limits
runConfig:
  cpu: 1
  memoryMiB: 512
  maxInstances: 2
